```commandline
kubectl apply -f prometheus/alertmanager/rules-configmap.yaml

kubectl apply -f prometheus/alertmanager/prom-configmap.yaml

kubectl apply -f prometheus/alertmanager/prom-deployment.yaml

kubectl apply -f prometheus/alertmanager/alert-configmap.yaml

kubectl apply -f prometheus/alertmanager/alert-deployment.yaml

```
